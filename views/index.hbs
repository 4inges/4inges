{{>navbar}}

<center>
    <h1>.:: INICIO ::.</h1>
</center>
<div class="nosotros">
    <div class="contenido">
    <h1>NOSOTROS</h1>
    <h3>Somos una empresa en la cual nos dedicamos a la excelencia y a la calidad de nuestros servicios</h3>
    </div>
    {{>btnubicacion}}
    <br>
    <br>
    {{>btncita}}
</div>

{{>footer}}

<div id="alert" style="position: fixed; width:100vw; height:30vh; top:91vh"></div>

<script>
    let alertX = document.getElementById('alert')

    const params = new URLSearchParams(window.location.search)
    if(params.has('fecha')){
        alertX.innerHTML =
        `<div class="alert alert-dismissible alert-success">
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            <strong>¡Cita agendada!</strong> Horario: ${params.get('hora')}, Fecha: ${params.get('fecha')}, Total: $${params.get('total')}.
        </div>`
    }
</script>